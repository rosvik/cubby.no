[package]
name = "cubby-no"
version = "0.1.0"
edition = "2021"

[dependencies]
dotenv = "0.15.0"
rocket = "0.5.1"
rocket_dyn_templates = { version = "0.2.0", features = ["handlebars"] }
serde = { version = "1.0.210", features = ["derive"] }
